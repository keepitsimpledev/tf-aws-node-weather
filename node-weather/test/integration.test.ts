describe("weather test", () => {
  test("placeholder", async () => {
    // arrange
    const realTestsInThisClass = 0;

    // act

    // assert
    expect(realTestsInThisClass).toEqual(0);
  });
});
